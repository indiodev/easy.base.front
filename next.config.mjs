/** @type {import('next').NextConfig} */
const nextConfig = {
    //swcMinify: false,
    // experimental: {
    //     workerThreads: false,
    //     cpus: 1,
    // }
};

export default nextConfig;
